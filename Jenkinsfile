pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Learn DevOps'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}